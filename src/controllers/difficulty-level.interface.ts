/* import {IApiInterface} from 'tools/api-doc';
import {apiUrls} from 'src/urls';
import DifficultyLevelModelInterface from 'src/models/difficulty-level.interface';

const urls = apiUrls.difficultyLevel;

export default {
    prefix: urls.prefix,
    methods: [
        {
            url: urls.getDifficultyLevels,
            method: 'get',
            returns: `${DifficultyLevelModelInterface.name}[]`
        }
    ]
} as IApiInterface;
 */